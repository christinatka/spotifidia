{
  "name": "spotifidia-server",
  "version": "1.0.0",
  "scripts": {
    "build": "cd react-ui/ && yarn && yarn build",
    "start": "node server",
    "start-dev": "nodemon index.js"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.3"
  }
}
